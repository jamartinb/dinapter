An example based on e020 (but without availability messages) where the operation names have been changed to take advantage of ''WordNet::Similarity''.
If it's run through ''dinapter.py'', it will return a proper solution, otherwise (through original Dinapter.jar), it can return any combination between the client and server branches.
