 This example is the same as supplier-1 but with several renames.

The results are as follows:
 1. Several correct contracts (equivalent ones).
 3. A useless contract (and equivalent ones) where, whatever the client does, it aborts the server behavior.

This example is used in WCAT'08.
